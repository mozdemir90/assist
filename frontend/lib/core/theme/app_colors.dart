import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors (Derived from ODAK Logo)
  static const Color primaryBlue = Color(0xFF1E82CB);
  static const Color primaryGreen = Color(0xFF38A169);

  // Accents
  static const Color accentPurple = Color(0xFF8B5CF6);
  static const Color errorRed = Color(0xFFEF4444);

  // Light Theme Colors
  static const Color backgroundLight = Color(0xFFFFFFFF); // Logoyla uyumlu olması için tam beyaz
  static const Color surfaceLight = Color(0xFFFFFFFF);
  static const Color textPrimaryLight = Color(0xFF1E293B);
  static const Color textSecondaryLight = Color(0xFF64748B);

  // Dark Theme Colors
  static const Color backgroundDark = Color(0xFF0F172A);
  static const Color surfaceDark = Color(0xFF1E293B);
  static const Color textPrimaryDark = Color(0xFFF8FAFC);
  static const Color textSecondaryDark = Color(0xFFCBD5E1);
}
